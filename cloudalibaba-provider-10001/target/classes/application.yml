
spring:
  application:
    name: nacos10000
  cloud:
    nacos:
      discovery:
        #nacos 服务注册中心地址
        server-addr: localhost:8848
server:
  port: 10001
management:
  endpoints:
    web:
      exposure:
        exclude: "*"